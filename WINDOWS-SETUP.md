# تثبيت نظام المحل على كمبيوتر المحل (ويندوز) — خطوة بخطوة

> دليل عملي للتجهيز من الصفر على جهاز ويندوز. النظام **محلي بالكامل** (Web App يفتح في المتصفح)،
> الداتا على نفس الجهاز، ومافيش أي اعتماد على الإنترنت بعد التثبيت.

---

## 🧰 أولًا: البرامج المطلوبة (تتثبّت مرة واحدة)

1. **Node.js 24**
   - حمّله من <https://nodejs.org> (نسخة 24 LTS) وثبّته (Next → Next → Install).
   - للتأكد: افتح **Command Prompt** واكتب `node -v` → لازم يطلع `v24.x`.
   - ⚠️ مهم: لازم **24** (النظام بيستخدم `node:sqlite` اللي مش موجود في النسخ الأقدم).

2. **pnpm** (مدير الحزم)
   - في الـ Command Prompt اكتب:
     ```
     npm install -g pnpm
     ```
   - للتأكد: `pnpm -v`.

3. **Git** (عشان تجيب الكود وتحدّثه لاحقًا)
   - حمّله من <https://git-scm.com/download/win> وثبّته (الافتراضيات تمام).

---

## 📥 ثانيًا: تجهيز الكود (أول مرة بس)

1. اختار مكان للمشروع، مثلًا `C:\POS`. افتح Command Prompt واكتب:
   ```
   cd C:\
   git clone https://github.com/Usama1917/Local-First-Pos.git POS
   cd POS
   ```

2. ثبّت الحزم:
   ```
   pnpm install
   ```
   (أول مرة بتاخد دقايق — بتنزّل كل المكتبات. محتاج إنترنت في الخطوة دي بس.)

---

## ▶️ ثالثًا: تشغيل النظام (كل يوم)

محتاج **نافذتين** Command Prompt (واحدة للسيرفر وواحدة للواجهة):

**النافذة 1 — السيرفر (الـ API):**
```
cd C:\POS
set PORT=8080
pnpm --filter @workspace/api-server run dev
```
سيبها مفتوحة. لما تشوف `Server listening` يبقى تمام.

**النافذة 2 — الواجهة:**
```
cd C:\POS
set PORT=24730
set BASE_PATH=/
pnpm --filter @workspace/pos run dev
```
سيبها مفتوحة كمان.

**افتح المتصفح** (كروم/إيدج) على:
```
http://localhost:24730
```
هيطلبك **تسجيل دخول** → المستخدم `admin` وكلمة المرور `admin123` (غيّرها بعد كده).

> للتأكد إن السيرفر شغّال: افتح `http://localhost:8080/api/healthz` لازم يطلع `{"status":"ok"}`.

---

## 💾 رابعًا: مكان حفظ البيانات (اختياري بس مهم)

افتراضيًا الداتا بتتحفظ جوّه `C:\POS\data\store.db`.
**الأفضل تحطها برّه مجلد المشروع** عشان التحديثات ما تلمسهاش — بطريقتين:

- **من الإعدادات داخل البرنامج**: الإعدادات → النسخ الاحتياطي → «مكان حفظ قاعدة البيانات» → اكتب مسار (مثلًا `D:\POS-Data`) → احفظ → **أعد التشغيل**.
- **أو** قبل التشغيل في النافذة 1 اكتب: `set POS_DATA_DIR=D:\POS-Data` ثم شغّل السيرفر.

> لو عندك بيانات قديمة، انقل ملف `store.db` للمكان الجديد قبل التشغيل.

---

## 🛟 خامسًا: النسخ الاحتياطي

من **الإعدادات → النسخ الاحتياطي → «إنشاء نسخة احتياطية»**:
- بيعمل **ملف مضغوط `.zip`** فيه قاعدة البيانات، بيتحفظ في `…\data\backups\`،
- والمتصفح **بيسألك تحفظه فين** (نزّله على فلاشة مثلًا).
- كل النسخ القديمة بتظهر في القائمة وقدّام كل واحدة زرار **تنزيل**.

> اعمل نسخة احتياطية **يوميًا** على فلاشة أو هارد خارجي.

---

## 🔄 سادسًا: تحديث النظام لاحقًا (لما يطلع تحديث)

1. **اعمل نسخة احتياطية الأول** (من البرنامج).
2. اقفل النافذتين، وافتح Command Prompt:
   ```
   cd C:\POS
   git pull
   pnpm install
   ```
3. شغّل النظام عادي (ثالثًا).
> `git pull` **مش بيلمس بياناتك** لأن ملفات الداتابيز متجاهلة من المستودع.

---

## ❗ حل المشاكل السريع

| المشكلة | الحل |
|---|---|
| `node:sqlite ... No such built-in module` | نسخة Node قديمة — ثبّت Node 24. |
| الصفحة مش بتفتح | اتأكد إن **النافذتين** شغّالين، وإن العنوان `http://localhost:24730`. |
| السيرفر مش شغّال | جرّب `http://localhost:8080/api/healthz`. لو مفيش رد، أعد تشغيل النافذة 1. |
| البورت مشغول | اقفل أي نسخة قديمة شغّالة، أو غيّر `PORT`. |

---

## 🤖 ملاحظة: تشغيل بضغطة واحدة (مستقبلًا)

ينفع نعمل ملفين `.bat` يسهّلوا الموضوع (مش متعملين دلوقتي):
- **`Start POS.bat`** → يفتح السيرفر + الواجهة + المتصفح بضغطة واحدة (للاستخدام اليومي).
- **`Update POS.bat`** → يعمل نسخة احتياطية ثم `git pull` + `pnpm install` (للتحديث).

اطلبهم وأعملهملك جاهزين.
